pre-commit:
  parallel: true
  jobs:
    - name: yamllint
      glob: "*.{yml,yaml}"
      run: yamllint {staged_files}
    - name: shellcheck
      glob: "*.sh"
      run: shellcheck --severity=style {staged_files}
